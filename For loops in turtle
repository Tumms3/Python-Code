#This code will draw out whichever letter you ask it too and however many time you request it too
#For short code tidings, I have shortened it down to just three letters
#Beginning of Code
from time import sleep
import turtle
t=turtle.Pen()
#Asks user which shape they wish to use
shape=input("What shape would you like to draw?" )
#Confirms user choice
print("You chose the letter", shape , )
#Asks user how many times they wish to draw said decision
howmany=int(input("How many times would you like this drawn?") )
#Confirms that it will draw the corrosponding shape
print("I will now draw your shape!")
#This code spells "N"
if shape == 'N':
    for x in range(0,howmany):
        t.left(90)
        t.forward(100)
        t.right(140)
        t.forward(130)
        t.left(140)
        t.forward(100)
        t.left(20)
elif shape == 'O':
#This code spells a "O"
    for x in range(0,howmany):
        t.circle(25)
        t.left(20)
#This code spells out a "A"
if shape == 'S':
    for x in range (0, howmany):
        t.left(90)
        t.forward(100)
        t.right(90)
        t.forward(100)
        t.right(90)
        t.forward(100)
        t.right(90)
        t.forward(100)
        t.left(20)
